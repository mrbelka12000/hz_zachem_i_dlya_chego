http:
  port: "8080"

postgres:
  host: "localhost"
  port: 5432
  user: "postgres"
  password: "postgres"
  dbname: "hz_zachem_storage"
  sslmode: "disable"

rabbitmq:
  url: "amqp://guest:guest@localhost:5672/"
  exchange: "events"
  queue: "hz_zachem_queue"

worker:
  interval: "1h"
