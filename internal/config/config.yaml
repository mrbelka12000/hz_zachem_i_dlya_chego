http:
  port: "8080"

postgres:
  host: "localhost"
  port: 5432
  user: "postgres"
  password: "postgres"
  dbname: "<service_name>_storage"
  sslmode: "disable"

rabbitmq:
  url: "amqp://guest:guest@localhost:5672/"
  exchange: "events"
  queue: "<service_name>_queue"

worker:
  interval: "1h"
